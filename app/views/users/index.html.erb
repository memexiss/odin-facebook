<h1 class="mb-4">Members</h1>

<div class="list-group">
  <% @users.each do |user| %>
    <% unless user == current_user %>
      <div class="list-group-item">      
      <%= link_to user.name, user_path(user), class: "list-group-item-action" %>
      <%= link_to "Follow", friendships_path(:friend_id => user), :method => :get, class: "float-right" %>
      </div>
    </p>
  <% end %>
  <% end %>
</div>