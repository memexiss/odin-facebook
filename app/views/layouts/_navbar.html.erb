<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Odin Facebook</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
    <% if user_signed_in? %>
    <%= link_to 'My Posts', user_path(current_user), class: "nav-item nav-link" %>
    <% end %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
    <p class="navbar-text float-right form-inline">
        <% if user_signed_in? %>
            Logged in as <strong><%= current_user.name %></strong>.
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'btn btn-outline-primary' %> |
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-outline-danger'  %>
        <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-outline-success'  %> |
            <%= link_to "Login", new_user_session_path, :class => 'btn btn-outline-primary'  %>
        <% end %>
    </p>

    </ul>
  </div>
</nav>